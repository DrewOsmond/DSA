// Input: s = "3[a]2[bc]"
// Output: "aaabcbc"

function decodeString(s: string): string {
  let stringDecoded = "";
  let i = 0;
  let j = 0;
  let k = 0;

  while (i < s.length) {
    const charAti = s[i];
  }
  return stringDecoded;
}
